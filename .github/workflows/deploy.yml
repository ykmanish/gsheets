name: Deploy to AWS

on:
  push:
    branches:
      - main
  # Manual trigger, so a deploy can be re-run from the Actions tab without
  # having to push another commit.
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ubuntu
          key: ${{ secrets.AWS_SSH_KEY }}
          script: |
            cd ~/apps/gsheets
            ./deploy.sh